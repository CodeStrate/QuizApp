import React from "react";
import ReactDOM from "react-dom/client";
import App from "./TitleScreen.jsx";
// import Quiz from './components/QuizScreen.jsx'

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
